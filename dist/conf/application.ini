# I'd rather not have it at all, since any decent init system won't need it anyway
-Dpidfile.path=/tmp/reactive-kitten-proxy.pid

# This is fine since the project is currently using an in-memory database
-Dplay.evolutions.db.default.autoApply=true

# Enter your Cat API key here if you have one. If not then comment out this field (the app will function without an API key)
#-Dkittenproxy.apikey=
